(() => {
    window.scrollTo(0, document.body.scrollHeight);

    const originalNow = Date.now;
    const start = originalNow();

    Date.now = function() {
        return start + (originalNow() - start) * 100;
    };

    const originalSetTimeout = window.setTimeout;
    window.setTimeout = function(callback, delay, ...args) {
        return originalSetTimeout(callback, delay / 100, ...args);
    };

    const originalSetInterval = window.setInterval;
    window.setInterval = function(callback, delay, ...args) {
        return originalSetInterval(callback, delay / 10, ...args);
    };

    console.log("🎉 Bypass yeumoney by Konmeo Hub🎉\n Báo lỗi/theo dõi tôi tại đây:\nDiscord server: discord.gg/huneipa\nDiscord:@konmeo22132\nYouTube:https://youtube.com/@huneee205");
})();
